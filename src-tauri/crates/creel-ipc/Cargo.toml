[package]
name = "creel-ipc"
version = "0.1.1"
description = "Small, versioned command protocol shared by DCreel native components"
edition = "2024"
license = "Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
